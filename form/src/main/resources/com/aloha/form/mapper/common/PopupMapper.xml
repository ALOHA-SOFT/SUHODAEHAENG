<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aloha.form.mapper.common.PopupMapper">

    <select id="listWithParams"  resultType="Popups">
        SELECT *
        FROM popups
        WHERE 1=1
        <if test="queryParams.search != null and queryParams.search != ''">
            AND (
                name LIKE CONCAT('%', #{queryParams.search}, '%')
                OR type LIKE CONCAT('%', #{queryParams.search}, '%')
                OR content LIKE CONCAT('%', #{queryParams.search}, '%')
            )
        </if>
        <if test="type != null and type != ''">
            AND type = #{type}
        </if>
        <if test="isShow != null">
            AND is_show = #{isShow}
        </if>
        ORDER BY 
        <choose>
            <when test="sort != null and sort != ''">
                <choose>
                    <when test="sort == 'name'">name</when>
                    <when test="sort == 'type'">type</when>
                    <when test="sort == 'isShow'">is_show</when>
                    <when test="sort == 'startedAt'">started_at</when>
                    <when test="sort == 'endedAt'">ended_at</when>
                    <when test="sort == 'createdAt'">created_at</when>
                    <otherwise>no</otherwise>
                </choose>
                <choose>
                    <when test="order != null and order == 'asc'">ASC</when>
                    <otherwise>DESC</otherwise>
                </choose>
            </when>
            <otherwise>no DESC, created_at DESC</otherwise>
        </choose>
    </select>

</mapper>
