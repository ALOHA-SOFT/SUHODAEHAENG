<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aloha.form.mapper.NoticeMapper">

    <resultMap id="NoticeMap" type="Notice">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="author" property="author"/>
        <result column="content" property="content"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="listWithParams" parameterType="map" resultMap="NoticeMap">
        SELECT *
        FROM notices
        <where>
            <if test="queryParams.search != null and queryParams.search != ''">
                AND (title LIKE CONCAT('%', #{queryParams.search}, '%')
                OR author LIKE CONCAT('%', #{queryParams.search}, '%')
                OR content LIKE CONCAT('%', #{queryParams.search}, '%'))
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY created_at DESC
    </select>

</mapper>