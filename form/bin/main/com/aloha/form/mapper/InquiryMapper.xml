<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aloha.form.mapper.InquiryMapper">

    <resultMap id="InquiryMap" type="Inquiry">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="status" property="status"/>
        <result column="reply_content" property="replyContent"/>
        <result column="reply_at" property="replyAt"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="listWithParams" parameterType="map" resultMap="InquiryMap">
        SELECT *
        FROM inquiries
        <where>
            <if test="queryParams.search != null and queryParams.search != ''">
                AND (name LIKE CONCAT('%', #{queryParams.search}, '%')
                OR title LIKE CONCAT('%', #{queryParams.search}, '%')
                OR content LIKE CONCAT('%', #{queryParams.search}, '%'))
            </if>
            <if test="status != null and status != '' and status != '전체'">
                AND status = #{status}
            </if>
        </where>
        ORDER BY created_at DESC
    </select>

</mapper>